require 'spec_helper'

describe ActiverecordUnitwise do
  it 'has a version number' do
    expect(ActiverecordUnitwise::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
